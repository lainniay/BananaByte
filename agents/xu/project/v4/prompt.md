## v1 -> try1
"任务：请完全移除分割结果标注的[main object]，在标注的 [reflective layer] 中，识别与[main object]不搭配的区域，即不像是主题后该有的背景"
                    "仅根据该区域生成反射出来的场景的想象（即镜中影像）"
merge_prompt = (
        "要求：根据原图(第一张图，含主体)和生成的反射层想象（第二张图），负叠加到原图，使得原图的反射层被抵消，剩余部分，也即正常背景, 确保表现正常"
        "确保主体不要动"
    )

## v2 -> try3
"任务：移除图像中标记为 [reflective layer] 的区域，同时移除标记为 [main object] 的区域，先想象[main object]移除后缺失的部分，补全没有反射的情况下可能的背景，再扩展背景到整个图像，最后仅保留[reflective layer]区域内的想象补全结果。"
                    "其他区域最好填为白色，便于下一步合成"

merge_prompt = (
        "要求：根据原图(第一张图，含主体)和生成的背景想象（第二张图），利用深色（取二者中颜色深的）覆盖到原图，使得原图的反射层被抵消，剩余部分，也即正常背景, 确保表现正常"
        "确保主体不要动"
    )

## v3 -> try4
 "任务：请完全移除分割结果标注的[main object]，和 [reflective layer]，仅根据剩下的区域生成背景底图。"

 merge_prompt = (
        "任务：你在执行去反射"
        "要求：根据原图(第一张，含主体)和生成的反射图像（第二张），用原图减去反射图像（类似ps中减去图层），使得原图的反射层被抵消，剩余部分是主体和正常背景"
        "根据剩余部分生成完整的图像，要求主体清晰，背景自然衔接，成功完成去反射"
    )

## v4 -> try5
 "任务：请完全移除分割结果标注的[main object]，和 [reflective layer]，仅根据剩下的区域生成背景底图。"


 merge_prompt = (
    "任务：你在执行去反射"
            "要求：根据原图(第一张，含主体)和生成的反射图像（第二张），用原图减去反射图像（类似ps中减去图层），使得原图的反射层被抵消，剩余部分是主体和正常背景"
            "根据剩余部分生成完整的图像，要求主体清晰，背景自然衔接，成功完成去反射"
